id: IMP-0013-03
change: CHG-0013
proposal: PROP-0013-05
attempt: 3
started_at: '2026-09-17T11:31:50Z'
actor:
  type: ai
  id: claude
  display_name: Claude Code
  ai:
    provider: anthropic
    tool: Claude Code
    model: claude-sonnet-5
approach: >-
  Fix the double-movement defect independent review found in IMP-0013-02 (attempt 2, recorded blocked pending
  approval, not passed), continuing directly on claude/chg-0013-gameplay-batch.
  BJ.Physics.applyBrickReboundZip's signature (ball, targetSpeed, dt, config) is unchanged; internally it now
  computes bonusSpeed = exitSpeed - incomingSpeed (always >= 0 by construction of
  exitSpeed = Math.max(incomingSpeed, zipSpeed)) and, only when bonusSpeed > 0, applies the supplemental position
  update scaled by bonusSpeed - using the outgoing unit direction (ball.vx/exitSpeed, ball.vy/exitSpeed) - rather
  than the full exitSpeed. Replaced the "moves the ball immediately" test with one asserting the displacement
  equals exactly (exitSpeed - incomingSpeed) * dt, is positive for a below-floor bounce, and is smaller than a
  full target-speed movement step; added a new test proving zero supplemental displacement when the ball is
  already at or above the floor. The existing game-level A/B displacement test, and all zip-floor/no-ratchet/
  paddle-independence/regenerating-brick/frenzy-timeout/resolveBallRect tests, are unchanged and re-verified.
  batty-joe-dev-spec-v1.13.0.yaml's non_regression block (corrected in IMP-0013-02) is left untouched; only the
  physics.brick_rebound_zip.observable_displacement prose is updated to describe the corrected mechanism. No
  change to Game.prototype.updateBalls's step ordering or to any file outside PROP-0013-05's declared scope.
source_revision:
  system: git
  before: 5fec1961f03045503a979818a1911d76ff4d2c24
  after: a1d17a69e3413d8b46bfc41805c8030601361af4
completed_at: '2026-09-17T11:42:23Z'
outcome: passed
blocker: >-
  As with IMP-0013-02: this attempt's own checks (below) all passed, but PROP-0013-05 had not yet received a
  human approval bound to its exact digest. Per source-of-truth.md rules 6, 8 and 9, this attempt was recorded
  blocked, not passed, while that approval was outstanding - specforge-authorization.py / validate-specforge.py
  correctly reported unauthorized_material_changes for this attempt's working-tree diff as a direct, expected
  consequence, not a defect to work around.
blocker_resolved: Gary Burwood explicitly approved PROP-0013-05 (APR-0015, mechanically bound to its exact
  canonical digest, verified via specforge-lifecycle.py transition CHG-0013 --to approved). This attempt's
  source_revision.before is corrected here from the tip of the prior in-progress commit to the trusted material
  baseline (5fec1961f03045503a979818a1911d76ff4d2c24) it was always actually measured against - this attempt was
  never re-implemented, only unblocked - and outcome is updated from blocked to passed now that a valid
  approval exists. Its test/validation evidence below was accurate throughout and is unchanged.
tests:
  status: passed
  passed:
  - 'batty-joe-tests.js via batty-joe-tests.html (Chromium/Playwright, headless): 128/128 tests passed (127
    from IMP-0013-02 plus 1 net new test for this attempt - one test rewritten, one added; 0 failures)'
  - node tests/run-v170-tests.js
  - node tests/run-chg0004-tests.js
  - node tests/run-chg0005-tests.js
  - node tests/run-chg0009-tests.js
  - node tests/run-physics-v160-tests.js
  - python3 tests/run-adoption-tests.py
  - python3 tests/run-risk-classifier-tests.py (23/23)
  failed: []
validation_checks:
- name: brick_rebound_zip_proportionate_displacement
  required: true
  status: passed
  evidence: 'New/revised tests prove: a below-floor bounce''s immediate displacement equals exactly
    (exitSpeed - incomingSpeed) * dt, is positive, and is smaller than a full target-speed movement step; a
    ball already at or above the zip floor receives exactly zero supplemental x/y displacement; the existing
    game-level A/B test (zip on vs off) still shows the zip-enabled case measurably farther, by the corrected,
    proportionate margin.'
- name: brick_rebound_zip_unchanged_properties
  required: true
  status: passed
  evidence: All zip-floor formula, no-ratchet, non-reduction-of-a-faster-ball, disabled-config,
    paddle-independence and next-frame-return-to-normal tests from IMP-0013-01/IMP-0013-02 pass unmodified.
- name: specification_accuracy
  required: true
  status: passed
  evidence: batty-joe-dev-spec-v1.13.0.yaml's non_regression block (corrected in IMP-0013-02) is unchanged;
    physics.brick_rebound_zip.observable_displacement now describes the bonus-only mechanism accurately; file
    parses as valid YAML.
- name: no_regression
  required: true
  status: passed
  evidence: Full batty-joe-tests.js suite (128/128), tests/run-v170-tests.js, tests/run-chg0004-tests.js,
    tests/run-chg0005-tests.js, tests/run-chg0009-tests.js, tests/run-physics-v160-tests.js,
    tests/run-adoption-tests.py and tests/run-risk-classifier-tests.py all pass. bounceFromPaddle and
    decayPaddleSpeedBoost function bodies remain byte-for-byte unmodified. applyBrickReboundZip's and
    handleBallBrickCollisions's signatures are unchanged from IMP-0013-02.
- name: specforge_repository_validation
  required: true
  status: passed
  evidence: 'python3 specforge/core/tools/validate-specforge.py . passed (213 canonical records), now that
    APR-0015 authorizes this material state. specforge-authorization.py check --json reports authorized:true,
    authorized_by referencing IMP-0013-03/APR-0015, and material_differences containing exactly the six files
    PROP-0013-05''s scope touches: batty-joe-config.js, batty-joe-dev-spec-v1.13.0.yaml, batty-joe-game.js,
    batty-joe-physics.js, batty-joe-tests.js, specforge/project.yaml. This check was recorded failed earlier in
    this same attempt (before APR-0015 existed) and is updated here to reflect the now-authorized state, per
    blocker_resolved above - not silently changed without explanation.'
notes: IMP-0013-02 is preserved unmodified as historical evidence; its "blocked" verdict was accurate for its
  own defect (the full-step double movement) and is superseded, not overwritten, by this attempt's corrected,
  proportionate displacement.
