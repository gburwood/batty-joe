id: IMP-0012-01
change: CHG-0012
proposal: PROP-0012-01
attempt: 1
started_at: '2026-09-15T14:36:00+01:00'
completed_at: '2026-09-15T15:17:00+01:00'
actor:
  type: ai
  id: chatgpt
  display_name: ChatGPT
  ai:
    provider: openai
    tool: ChatGPT
    model: gpt-5.6-sol
approach: >-
  Implement the approved non-authoritative retrospective risk classifier entirely outside specforge/core and specforge/packs. The classifier consumes an executable YAML policy, inspects actual Git diffs for passed historical implementation source revisions, excludes governance bookkeeping/transient paths consistently with current material-authority behaviour, and emits deterministic LOW/MEDIUM/HIGH/BLOCKED results. No lifecycle, authorization, product or trusted-authority behaviour is changed.
source_revision:
  system: git
  before: cc5525f60a5503acc1720a0f67d95e46fb7ddcbe
outcome: blocked
tests:
  status: blocked
  passed:
  - Focused deterministic classifier policy suite passed 10/10 against byte-identical branch blobs.
  failed: []
  blocked:
  - Full historical classifier execution in the ChatGPT sandbox lacked a local Git checkout/history.
  - Existing Batty Joe regression harness set was not executed in the ChatGPT sandbox.
  - SpecForge repository validation was not executed in the ChatGPT sandbox.
validation_checks:
- name: risk_classifier_unit_tests
  required: true
  status: passed
  evidence: Focused policy suite passed 10/10. Verification also caught and corrected hidden-file path normalization before this attempt was blocked.
- name: retrospective_chg0001_to_chg0011
  required: true
  status: blocked
  evidence: >-
    Preliminary path-based analysis predicted CHG-0001..0009 HIGH, CHG-0010 LOW and CHG-0011 MEDIUM. A separately executed exploratory classifier reported by the user produced CHG-0001..0009 HIGH, CHG-0010 MEDIUM and CHG-0011 MEDIUM and exposed two policy-design issues: routine bounded specforge.yaml specification-pointer updates can dominate classification, and edits to existing regression harnesses should not be LOW merely because they live under tests/.
- name: existing_regression_harnesses
  required: true
  status: blocked
- name: specforge_validation
  required: true
  status: blocked
blocker: >-
  PROP-0012-01 requires CHG-0010 to classify LOW and explicitly requires a proposal revision before implementation continues if an unexpected result requires a policy change. The next experiment run now requires deterministic semantic handling for bounded legacy specforge.yaml pointer updates and MEDIUM classification for edits to existing test/regression harnesses. Continuing under PROP-0012-01 would exceed the approved proposal.
notes: >-
  This attempt remains useful historical evidence and is not rewritten as passed. The experimental files created during the attempt remain on the dedicated implementation branch but are not authoritative and must not be treated as completion evidence. PROP-0012-02 is required before those files may be revised for the second controlled run.
